/// iOS PrayerCategory.swift 대응

class PrayerCategory {
  final int id;
  final int categoryCode;
  final String categoryName;

  const PrayerCategory({
    required this.id,
    required this.categoryCode,
    required this.categoryName,
  });
}
